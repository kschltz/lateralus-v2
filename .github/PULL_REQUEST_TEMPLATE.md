<!--
Agent / author instructions (do not delete this comment block):

Fill every section below before requesting review. Empty sections, "N/A"
without reason, or unverifiable claims ("tested locally", "works for me")
are incomplete.

Evidence must be verifiable by a reviewer who was not in the session:
commands + pasted output, CI links, test names that pass, committed
artifacts, or embedded images/videos of the running behavior. Screenshots
and screen recordings are encouraged for UI / CLI / interactive changes.
-->

## Intent

<!-- Why does this change exist? What problem or goal does it serve?
     State the user/operator outcome, not just the mechanism. -->

-

## Alternatives considered

<!-- List real options you evaluated (including "do nothing" when relevant).
     For each alternative: what it would look like, and why it was rejected
     or deferred. At least one alternative besides the chosen path. -->

| Alternative | Reasoning |
|-------------|-----------|
| | |
| | |

## Path chosen

<!-- Which alternative did we take, and why is it the best fit *now*?
     Call out trade-offs, scope cuts, and follow-ups explicitly deferred. -->

-

## Validation & regression guards

<!-- How do we know this stays correct? Name concrete guards:
     unit/integration/e2e tests, lint/rg gates, schema asserts, halt/lifecycle
     checks, air-gap/default-off invariants, etc. Prefer executable guards
     over prose-only checklists. -->

| Guard | How it is enforced | Suite / command |
|-------|--------------------|-----------------|
| | | |

## Acceptance criteria

<!-- Checklist of observable outcomes that mean "done". Each item should be
     falsifiable (a reviewer can tell pass vs fail). -->

- [ ]
- [ ]
- [ ]

## Evidence of working code

<!-- ONLY verifiable evidence counts. Unverifiable claims do not satisfy this
     section.

     Acceptable evidence (use as many as apply):
     - Commands run + relevant stdout/stderr (paste or link)
     - CI run URL / check name that is green on this branch
     - Specific test namespaces/names that pass, with the command used
     - Embedded screenshots or screen recordings of the behavior
     - Links to committed demo configs / golden fixtures exercised above

     Not sufficient alone:
     - "Tested locally" / "LGTM" / "works on my machine"
     - Intent or design prose without a run artifact
-->

### Commands / tests

```bash
# paste the exact commands used
```

```
# paste the relevant output (or link to CI / log artifact)
```

### Visual / interactive (if applicable)

<!-- Drag-and-drop or attach images/videos here. Describe what each shows. -->

-
